import React from "react"
import ReactDOM from "react-dom/client"
import LauncherUI from "./LauncherUI.jsx"
import "./index.css"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LauncherUI />
  </React.StrictMode>
)
